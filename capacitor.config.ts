import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.bolorundurowb.atheneum',
  appName: 'Atheneum',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
